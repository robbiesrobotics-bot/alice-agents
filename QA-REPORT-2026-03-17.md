# QA Report — 2026-03-17

> **Status:** ✅ COMPLETE
> **Run by:** Quinn (QA subagent)
> **Completed:** 2026-03-17 ~13:33 EDT

## Summary
- ✅ PASS: 18
- ⚠️ WARN: 2
- ❌ FAIL: 4

---

## Area 1 — API Endpoints

| Endpoint | Status Code | Result | Notes |
|----------|-------------|--------|-------|
| `GET /api/license/validate?key=ALICE-TEST-FAKE-0001` | 500 | ❌ FAIL | Empty body — should return 200 `{valid:false}` or 400, not 500 |
| `GET /api/license/validate` (no key) | 400 | ✅ PASS | `{"valid":false,"error":"No key provided"}` — correct |
| `POST /api/stripe/checkout` `{"plan":"pro"}` | 500 | ❌ FAIL | `{"error":"An error occurred with our connection to Stripe. Request was retried 2 times."}` |
| `POST /api/stripe/checkout` `{"plan":"cloud"}` | 500 | ❌ FAIL | Same Stripe connection error |
| `POST /api/waitlist` `{"email":"qa-test@av3.ai","plan":"starter"}` | 200 | ✅ PASS | `{"ok":true}` |
| `POST /api/waitlist` `{}` (missing email) | 400 | ✅ PASS | `{"error":"email required"}` — correct validation |

---

## Area 2 — Vercel Env Vars

| Var | Present |
|-----|---------|
| `RESEND_API_KEY` | ✅ Yes (Encrypted, Production) |
| `STRIPE_SECRET_KEY` | ✅ Yes (Encrypted, Production) |
| `STRIPE_WEBHOOK_SECRET` | ✅ Yes (Encrypted, Production) |
| `STRIPE_PRO_PRICE_ID` | ✅ Yes (Encrypted, Production) |
| `STRIPE_CLOUD_PRICE_ID` | ✅ Yes (Encrypted, Production) |

> **Note:** All 5 env vars are present in Vercel Production. The Stripe 500s are NOT due to missing env vars — likely a bad price ID or Stripe key mismatch. Needs investigation at the Stripe dashboard level.

---

## Area 3 — npm Package

Package: `@robbiesrobotics/alice-agents@1.2.9` (28 files, 85.8 kB unpacked)

| Check | Result | Notes |
|-------|--------|-------|
| `templates/agents-pro.json` NOT in file list | ✅ PASS | Not present in tarball — Pro templates correctly excluded |
| `templates/agents-starter.json` IS present | ✅ PASS | `5.9kB templates/agents-starter.json` confirmed |
| `LICENSE` file is present | ✅ PASS | `1.1kB LICENSE` confirmed |

---

## Area 4 — Page Content

| Page | Check | Result | Notes |
|------|-------|--------|-------|
| Homepage | Contains `npx @robbiesrobotics/alice-agents` | ✅ PASS | Present multiple times |
| Homepage | Contains `id="install"` anchor | ✅ PASS | Link to `/#install` present; install section exists |
| Homepage | No `$29/mo` mention | ✅ PASS | Old pricing not found |
| Pricing | Contains `$49` (Pro one-time) | ✅ PASS | `$49one-time` present |
| Pricing | Contains `$19.99` (Cloud monthly) | ✅ PASS | `$19.99/month` present |
| Pricing | Contains `Free` (Starter tier) | ✅ PASS | Starter = Free, confirmed |
| Pricing | No `$29/mo` mention | ✅ PASS | Old pricing not found |
| Pricing | All three tiers present | ✅ PASS | Starter, Pro, Cloud Add-on all visible |
| Blog | Loads without error | ✅ PASS | HTTP 200 |
| Blog | At least 3 posts listed | ⚠️ WARN | Readability only extracted 1 post ("The AI Agent Landscape in 2026"). Could be JS-rendered or extractor limitation. Verify manually. |
| Signup | Form renders / email input | ⚠️ WARN | Readability extracted form shell but email input may be JS-rendered. Form heading appears ("Join the A.L.I.C.E. Pro waitlist"). |
| Signup | **No `$29/mo` mention** | ❌ FAIL | Page explicitly shows: **"Founder pricing locked at $29/mo"** — old pricing still live on signup page |
| Signup | No obvious crash content | ✅ PASS | Page loads, no 500/stack trace |
| Changelog | Loads without error | ✅ PASS | HTTP 200, v1.2.9 / v1.2.4 / v1.2.3 / v1.2.2 all listed |

---

## Area 5 — GitHub

| Check | Result | Notes |
|-------|--------|-------|
| `robbiesrobotics-bot/alice-agents` is public | ✅ PASS | Repo accessible without auth, README visible |
| Repo has LICENSE file | ✅ PASS | MIT license confirmed (npm pack shows 1.1kB LICENSE; README ends with "MIT") |
| `.github/ISSUE_TEMPLATE/bug_report.md` exists | ✅ PASS | Confirmed in directory listing |
| `.github/ISSUE_TEMPLATE/feature_request.md` exists | ✅ PASS | Confirmed in directory listing |

---

## Bugs (FAIL items)

### ❌ BUG-01 — Stripe checkout returning 500 (P0)
- **Description:** Both `/api/stripe/checkout` endpoints (plan=pro, plan=cloud) return HTTP 500 with Stripe connection error
- **Expected:** HTTP 200 with a Stripe checkout session URL
- **Actual:** `{"error":"An error occurred with our connection to Stripe. Request was retried 2 times."}`
- **Severity:** P0 — **Payments are completely broken. No revenue can be collected.**
- **Note:** Env vars ARE present in Vercel. Likely cause: STRIPE_SECRET_KEY is a test key and price IDs are from a different Stripe account/env, or price IDs are wrong/deleted. Check Stripe dashboard for live price ID mismatch.

### ❌ BUG-02 — Signup page shows old $29/mo founder pricing (P1)
- **Description:** `/signup` page displays "Founder pricing locked at $29/mo" — stale copy that contradicts the new pricing ($49 one-time Pro + $19.99/mo Cloud)
- **Expected:** Signup page should reflect current pricing ($49 one-time or $19.99/mo Cloud)
- **Actual:** `"Be first when Pro opens. Founder pricing locked at $29/mo."` + `"Pro Plan · $29/mo · Founder pricing"`
- **Severity:** P1 — Will confuse and mislead customers who come from the pricing page

### ❌ BUG-03 — License validate with invalid key returns 500 (P1)
- **Description:** `GET /api/license/validate?key=ALICE-TEST-FAKE-0001` returns HTTP 500 with empty body
- **Expected:** HTTP 200 `{"valid":false,"error":"Invalid license key"}` or HTTP 400
- **Actual:** HTTP 500, empty response body
- **Severity:** P1 — Suggests unhandled exception in license validation logic. CLI installers will fail silently on invalid keys.

### ⚠️ WARN-01 — Blog shows only 1 post (P2 / unconfirmed)
- **Description:** Blog index rendered only 1 post via web_fetch readability extraction
- **Expected:** At least 3 posts (changelog mentions "5 launch posts" added in v1.2.4)
- **Actual:** Only "The AI Agent Landscape in 2026" visible
- **Severity:** P2 — Likely JS-rendered content not captured by static fetch. **Verify manually in browser before escalating.**

---

## Recommendations

1. **Stripe debugging path:** Even though env vars exist, check the Stripe dashboard — verify `STRIPE_PRO_PRICE_ID` and `STRIPE_CLOUD_PRICE_ID` are valid price IDs in the active Stripe account matching the `STRIPE_SECRET_KEY`. The "retried 2 times" message suggests network/auth failure, not a missing env var.

2. **Signup page copy:** The `/signup` page appears to be a legacy waitlist page that was never updated when pricing changed. Update or replace the copy before any paid launch traffic hits it. The pricing page and homepage are correct — just the signup page is stale.

3. **License validate error handling:** The 500 on an invalid key suggests the route may be crashing when looking up a key that doesn't exist in the DB. Add a try/catch and return `{valid:false}` for unknown keys.

4. **Blog verification:** Manually open `/blog` in a browser to confirm all 5 launch posts are rendering. The web_fetch extractor may have missed them due to client-side rendering.

5. **npm package is clean:** v1.2.9 tarball looks good — starter template present, pro template excluded, LICENSE included. No issues.
