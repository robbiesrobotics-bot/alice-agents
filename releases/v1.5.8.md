# v1.5.8

Status: approved

## Summary

Fixes "Cannot find package 'got'" crash during gateway registration by replacing `got` with native `fetch` in alice-cloud CLI.

## Highlights

- alice-cloud CLI v1.1.0 — zero external HTTP dependencies
- Gateway registration, heartbeat, and status all use native fetch
- No more missing package errors during npx install flow

## Announcement

v1.5.8 fixes the gateway registration crash caused by missing `got` dependency.

## Channels

- npm: @robbiesrobotics/alice-agents@1.5.8
